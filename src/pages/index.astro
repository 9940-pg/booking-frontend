---
import Layout from "../layouts/Layout.astro";
import BookingApp from "../components/booking/BookingApp.jsx";

const service = Astro.url.searchParams.get("service");
---

<Layout>
  <BookingApp service={service || "theatre"} client:load />
</Layout>
